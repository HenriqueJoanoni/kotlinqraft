query GetCountriesByLanguages {
    languages(filter: { code: { in: ["en", "fr", "es"] } }) {
        name
        native
        rtl
        countries {
            name
            capital
        }
    }
}