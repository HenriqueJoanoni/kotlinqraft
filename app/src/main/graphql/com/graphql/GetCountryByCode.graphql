query GetCountryByCode {
    country(code: "US") {
        name
        capital
        continent {
            name
        }
        currencies
        languages {
            name
            native
        }
    }
}
