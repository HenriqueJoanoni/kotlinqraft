query GetSubdivisionByCountry {
    country(code: "US") {
        name
        states {
            name
            code
        }
        subdivisions {
            name
            emoji
        }
    }
}
